import React from 'react';
import styled from 'styled-components';

const Details = (props) => {
    return (
        <div>
            <h1> Details </h1>
        </div>
    )
}

export default Details;
